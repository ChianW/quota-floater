Pi extension: official remaining quota via Token Monitor probe.

Source:
  C:\Users\wangq\Desktop\quota-floater\pi-extension\quota.ts

Enabled (copied, not overwritten):
  %USERPROFILE%\.pi\agent\extensions\quota-floater.ts

In Pi:
  /reload
  /quota

This shells node collect.js. Agents can use `node collect.js --json`
instead (snapshot has no secrets). It does not install community quota
packages and does not print secrets.
